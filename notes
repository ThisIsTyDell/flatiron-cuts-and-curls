CUTS N CURLS RAILS APP NOTES

Domain - Hair Salon/Barbershop - Can be adapted to suit both

Models
------
USER
  has a name
  has a username
  has a password
    password is encrypted
  has many appointments
  (belongs to current_appointment, classname "appointment") - maybe
  has a role - client and employee on same table using a has_many | belongs_to - Self Join Table
    customer
    employee - roles
      stylist
      manager
      admin
  
APPOINTMENT
  has a time
  has a date
  (has a total price) - maybe
  belongs to a user
  has many AppointmentServices
  has many Services through AppointmentServices

CATEGORY
  has_many Services

SERVICES
  has a price
  has many AppointmentServices
  belongs to Category

APPOINTMENT_SERVICES - Join Table
  belongs to appointment
  belongs to Service



  FACEBOOK <li><%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %></li>

-------------------------
jQuery and AJAX notes

  must render index page using jQuery and Active Model Serialization JSON backend
    -convert Services index page to implement this

  DONE - must render show page using jQuery and Active Model Serilaization JSON backend
    -convert Services show page to implement this

  must reveal at least one has-many relationship in the JSON that is then rendered to the page.
    -convert users appointment page to use this

  Must use your Rails API and a form to create a resource and render the response without a page refresh
    -allow new services to be added without refreshing the page

  Must translate the JSON responses into Javascript Model Objects. The Model Objects must have at least one method on the prototype.