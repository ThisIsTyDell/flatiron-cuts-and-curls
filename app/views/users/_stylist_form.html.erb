<%= form_for(current_user) do |f| %>
  <%= f.label 'Select a Stylist' %><br>
  <%= f.select :stylist_id, User.employees.collect { |u| [ u.name, u.id ] } %><br>
  <%= f.submit :"Confirm", class: "btn-main" %>
<% end %>