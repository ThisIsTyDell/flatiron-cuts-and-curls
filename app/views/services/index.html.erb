<% @services.each do |service| %>
  <li><%= service.title %> - $<%= service.price %> - <%= link_to "Edit", edit_service_path(service) if current_user.admin? || current_user.employee? %></li>
<% end %>

<%= link_to "Add New Service", new_service_path if current_user.admin? || current_user.employee? %>